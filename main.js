var isFriend = "Toheed";
if (isFriend === "Toheed" || isFriend === "Abbas") {
    console.log("".concat(isFriend, " is your friend."));
}
else {
    console.log("".concat(isFriend, " is not your friend"));
}
